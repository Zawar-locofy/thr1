import styles from "./PalawanImageIcon.module.css";
const PalawanImageIcon = ({ palawanImage }) => {
  return <img className={styles.palawanimageIcon} alt="" src={palawanImage} />;
};

export default PalawanImageIcon;
